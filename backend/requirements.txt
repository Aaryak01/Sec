gradio
cohere
httpx
python-dotenv
scikit-learn
pandas
matplotlib
numpy
# Only used by fetch_stock_prices.py (run manually to refresh
# data/stock_prices.csv) — not imported by app.py/api.py at request time.
yfinance
fastapi
# [standard] pulls in uvloop/httptools for real production throughput —
# matters once this runs on a hosting platform instead of local dev.
uvicorn[standard]
pyjwt[crypto]
# boto3 talks to DynamoDB (conversation persistence) using AWS credentials
# resolved from the default provider chain — env vars (AWS_ACCESS_KEY_ID /
# AWS_SECRET_ACCESS_KEY) set on the hosting platform in production, or the
# AWS CLI credentials already configured on this machine for local dev. No
# keys are stored here.
boto3
# Stripe TEST MODE only (usage-limit/Pro-upgrade feature) — see the
# module-level comment in api.py above the stripe.api_key assignment.
stripe
# (no new dependency added here -- this comment only exists to trigger a
# deploy under backend/** and verify the OIDC trust policy fix, 3rd try --
# trust policy now uses the immutable-ID sub format that GitHub actually
# issues for this repo: repo:Aaryak01@224979162/Sec-Analyst-LLM@1315359009:*)
